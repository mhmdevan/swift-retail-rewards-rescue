NetworkingModern module.

Includes:
- `URLSession` async/await networking client
- endpoint contract abstraction
- modern offers repository implementation
- shared mapping into `OfferSummary`

This module is the modernized networking seam used by the SwiftUI wallet island.
